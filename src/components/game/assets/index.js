/**
 * Assets manager
 */
export default class Assets {
  /**
   * Construct
   * @param {Game} parent
   */
  constructor(parent) {
    this.parent = parent;
  }

  /**
   * Initialize
   */
  init() {}
}
