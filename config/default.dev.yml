input:
  keyboard:
    mapping:
      cursorUp: ArrowUp
      cursorDown: ArrowDown
      cursorLeft: ArrowLeft
      cursorRight: ArrowRight
      cameraUp: PageUp
      cameraDown: PageDown
  mouse:
  gamepad:


assets:
  scene:
    cache:
      maxSize: 3
  terrain:
    cache:
      maxSize: 3


logic:
  ai:
  gameplay:
    world:
      defaultScene: testGround
  gui:
    tile:
      inputWidth: 16
      inputHeight: 16
      inGameWidth: 128
      inGameHeight: 128
  online:


output:
  audio:
  graphic:
